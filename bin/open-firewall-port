#!/bin/bash
if [ ! "${1}" == "-p" ]; then
  echo "${0} must specify the Discovery port with the -p option"
  exit 1
fi
shift
DSC_PORT="${1}"
echo "Exposing the Discovery port ${DSC_PORT} in the firewall ..."
firewall-cmd --permanent --add-port=${DSC_PORT}/tcp
firewall-cmd --reload
